namespace LearningMissionLab
{
    /// <summary>
    /// Enum: Role
    /// Purpose: Describes user roles, used with <see cref="Account"/>.
    /// </summary>
    public enum Role
    {
        Unspecified,
        Admin,
        Coordinator,
        Accountant,
        Instructor,
        Student,
        Intern,
        BoardMember,
    }
}
