namespace LearningMissionLab.Models
{
    /// <summary>
    /// Enum: Status
    /// Purpose: Describes user account status values, used with
    /// <see cref="Account"/>.
    /// </summary>
    public enum Status
    {
        Unspecified,
        Pending,
        Active,
        Suspended,
        Closed,
    }
}
